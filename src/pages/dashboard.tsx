import styled from "styled-components";

const Dashboard = () => {
  for (let i = 0; i < 12; i++) {
    console.log(i);
  }

  return (
    <div>
      <h1>Bem vindo ao Dashboard</h1>
    </div>
  );
};

export default Dashboard;
